// Phase 5 replaces this with the real landing page.
export default function Home(): React.ReactElement {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1>Basira</h1>
      <p>API is up at <code>/api/v1/...</code></p>
      <p>Phase 5 will deliver the UI.</p>
    </main>
  );
}
